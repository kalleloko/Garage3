@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<ul class="navbar-nav">
    @if (User.Identity.IsAuthenticated)
    {
        @if (User.IsInRole("Admin"))
        {
            <li class="nav-item">
                <a class="nav-link text-dark">Users</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-dark">Vehicles</a>
            </li>
        }
        @if (User.IsInRole("Member") || User.IsInRole("Admin"))
        {
            <li class="nav-item">
                <a class="nav-link text-dark">My Vehicles</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-dark">Park Vehicle</a>
            </li>
        }
    }
</ul>